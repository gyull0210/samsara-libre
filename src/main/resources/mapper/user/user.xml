<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gyull.board.repository.user.UserRepository">
  <select id="select" resultType="com.gyull.board.domain.user.User">
    select * from user where id = #{id}
  </select>
</mapper>